package employees;

import folder.Employee;

public class Worker extends Employee {
    
    public String getPosition(double position) {
        return  position;
    
        public String toString(String fullName, String salary )  {
            return "Worker: " + getFullName + ", Salary: " + getSalary + "Psition" + position;
        }
}
